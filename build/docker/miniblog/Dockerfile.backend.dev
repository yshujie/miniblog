FROM golang:1.21-alpine

WORKDIR /app

# 安装基础工具
RUN apk add --no-cache git make

# 安装 air
RUN go install github.com/cosmtrek/air@latest

# 复制 air 配置文件
COPY .air.toml .

# 设置工作目录
WORKDIR /app

# 暴露端口
EXPOSE 8081

# 使用 air 启动服务
CMD ["air", "-c", ".air.toml"] 